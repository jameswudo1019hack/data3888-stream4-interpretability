# Exact versions used to produce every result in the report (Python 3.11).
# Install:  pip install -r requirements.txt
#
# Covers BOTH the training/analysis scripts (scripts/) and the Shiny app (app/).

# --- core analysis / training ---
torch==2.10.0
timm==1.0.25
grad-cam==1.5.5
scikit-learn==1.6.1
numpy==2.4.4
pandas==2.3.3
matplotlib==3.10.9
Pillow==12.1.1
tqdm==4.67.3

# --- interactive Shiny demo ---
shiny==1.6.1
shinyswatch==0.10.0
